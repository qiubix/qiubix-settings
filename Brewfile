# Brewfile — install with `brew bundle` (run by ./install.sh).
# Declarative replacement for the old bootstrap-mac.sh.

# ── Core CLI / dev tools ─────────────────────────────────────────
brew "git"
brew "gh"
brew "tmux"
brew "tmuxp"
brew "vim"
brew "neovim"
brew "tree-sitter-cli"  # required by nvim-treesitter (main branch) to build parsers
brew "antidote"        # zsh plugin manager (used by zsh/.zshrc)
brew "mise"            # primary version manager
brew "tree"
brew "btop"
brew "fzf"
brew "ripgrep"
brew "tldr"
brew "jq"
brew "pre-commit"
brew "go-task"
brew "adr-tools"

# ── Version managers (legacy, still referenced in zsh/env.zsh) ────
brew "tfenv"
brew "pyenv"
brew "jenv"
brew "python@3"

# ── JVM / build ──────────────────────────────────────────────────
# brew "maven"
# brew "gradle"

# ── Containers ───────────────────────────────────────────────────
brew "colima"
brew "docker"
brew "docker-compose"

# ── Casks: terminal, JVM, editors ────────────────────────────────
cask "ghostty"
cask "corretto@17"
cask "corretto@11"
cask "jetbrains-toolbox"
cask "basictex"

# ── Casks: apps (trim to taste) ──────────────────────────────────
cask "spotify"
cask "obsidian"
cask "todoist"
cask "timeular"
# cask "vanilla"
# cask "rocket"
cask "balance-lock"
# cask "rescuetime"
cask "sync"
