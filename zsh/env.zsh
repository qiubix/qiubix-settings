#!/bin/zsh
#
# env.zsh — generic, OS-neutral exported environment variables.
#
# Keep this file portable. Put machine/host/work-specific values in local.zsh
# (untracked); OS-specific values in os/macos.zsh and os/linux.zsh; PATH edits in
# path.zsh; version managers in runtimes.zsh.

# (add generic exports here)
